package main

// import brings external paxkages into the file that you are working
// where actually you need it
import (
	"fmt"
	"math"
)

func main(){
// packageName.functionName -> call a function from a package

	fmt.Println("sqrt(25)" , math.Sqrt(25));
}